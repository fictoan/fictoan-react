import { Meta, Story, Canvas } from "@storybook/addon-docs";
import * as stories from './FormItem.stories';
import { FormItem } from "./FormItem";
import { CommonArgTypes } from "../../../utils/storyUtils";

<Meta title="Components/FormItem" component={FormItem} argTypes={{ ...CommonArgTypes }} />

# FormItem

This is a FormItem component.

<Canvas>
    <Story story={stories.Default} />
</Canvas>
