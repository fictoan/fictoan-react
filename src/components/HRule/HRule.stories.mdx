import { Meta, Story, Canvas } from "@storybook/addon-docs";
import * as stories from './HRule.stories';
import { HRule } from "./HRule";
import { CommonArgTypes } from "../../utils/storyUtils";

<Meta title="Components/HRule" component={HRule} argTypes={{ ...CommonArgTypes }} />

# HRule

This is an HRule component.

<Canvas>
    <Story story={stories.Default} />
    <Story story={stories.Primary} />
    <Story story={stories.Secondary} />
    <Story story={stories.Tertiary} />
</Canvas>
