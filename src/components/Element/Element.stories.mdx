import { Meta, Story, Canvas } from "@storybook/addon-docs";
import * as stories from './Element.stories';
import { Element } from "./Element";
import { CommonArgTypes } from "../../utils/storyUtils";

<Meta title="Components/Element" component={Element} argTypes={{ ...CommonArgTypes }} args={{ as: "div" }} />

# Element

This is an Element component.

<Canvas>
    <Story story={stories.Default} />
</Canvas>
