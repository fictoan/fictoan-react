import { Meta, Story, Canvas } from "@storybook/addon-docs";
import * as stories from './PinInputField.stories';
import { PinInputField } from "./PinInputField";
import { CommonArgTypes } from "../../utils/storyUtils";

<Meta title="Components/PinInputField" component={PinInputField} argTypes={{ ...CommonArgTypes }} />

# PinInputField

This is a PinInputField component.

<Canvas>
    <Story story={stories.Default} />
</Canvas>
