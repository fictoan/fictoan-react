import { Meta, Story, Canvas } from "@storybook/addon-docs";
import * as stories from './Spinner.stories';
import { Spinner } from "./Spinner";
import { CommonArgTypes } from "../../utils/storyUtils";

<Meta title="Components/Spinner" component={Spinner} argTypes={{ ...CommonArgTypes }} />

# Spinner

This is a Spinner component.

<Canvas>
    <Story story={stories.Default} />
</Canvas>
